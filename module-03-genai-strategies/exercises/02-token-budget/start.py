"""
Exercise 02 — Token Budget
Token counting and budget enforcement for Pathfinder chat turns.
"""

from __future__ import annotations

# TODO: count_tokens(text: str) -> int
# TODO: enforce_budget(messages: list[str], max_tokens: int) -> list[str]
# - Keep the most recent messages that fit; document truncation policy.


def count_tokens(text: str) -> int:
    raise NotImplementedError("TODO")


def enforce_budget(messages: list[str], max_tokens: int) -> list[str]:
    raise NotImplementedError("TODO")
