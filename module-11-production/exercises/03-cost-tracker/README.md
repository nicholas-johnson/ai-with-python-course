# Exercise 03 — Cost tracker

**Mission briefing:** Track estimated tokens or dollars per session for Pathfinder chat. Enforce a hard stop or downgrade path when the budget is exceeded.

## Objectives

1. Record usage per model call (stub counts are fine).
2. Enforce a per-session budget with clear error or fallback behavior.
3. Expose a summary for a hypothetical ops dashboard.

## Run the tests

```bash
pytest module-11-production/exercises/03-cost-tracker/test_start.py -v
```
